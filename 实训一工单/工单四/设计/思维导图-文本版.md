# PDF 招股说明书智能问答系统 v4 — 思维导图（文本版）

> 工单：人工智能 NLP-RAG-图像内容解析及检索优化  
> 🟢 绿色标记 = v4 新增

```
                                    ┌─ 📋 系统概述
                                    │   ├── 🟢 RAG + 多模态视觉理解
                                    │   ├── 🟢 工单：图像内容解析及检索优化
                                    │   ├── 自然语言提问→检索/看图→生成
                                    │   └── 🟢 v3 所有能力 + 多模态图片问答
                                    │
                                    ├─ ⚡ 核心功能
                                    │   ├── 🟢 多模态图片问答 (组织结构图/IC市场增长图)
                                    │   ├── PDF 解析 MinerU (默认主后端)
                                    │   ├── 🟢 视觉PDF解析 (检测/渲染/提取)
                                    │   ├── BM25 + 向量 + RRF 混合检索
                                    │   ├── Redis 短期 + Milvus 长期记忆
                                    │   ├── 元数据直答 + 规则抽取 + LLM
                                    │   ├── 🟢 Milvus 文档存储
                                    │   └── 🟢 Redis 多模态缓存 (1h TTL)
                                    │
                                    ├─ 🏗 技术架构
                                    │   ├── Streamlit 前端 (port 8502)
            ┌─ PDF 招股说明书 ──────┼─  ├── 🟢 RAG引擎 mixin包 (rag_engine/ 6模块)
            │  智能问答系统 v4      │   ├── 🟢 HybridRetriever 包 (retriever/ 6模块)
            │                      │   ├── 🟢 小米 MiMo API (mimo-v2.5-pro)
            │                      │   ├── 🟢 MultimodalImageParser (mimo-v2.5)
            │                      │   ├── sentence-transformers 嵌入模型
            │                      │   └── Redis + Milvus (Docker)
            │                      │
            │                      ├─ 🟢 v4 新增功能
            │                      │   │
            │                      │   ├─ 🌟 多模态图片问答
            │                      │   │   ├── 🏢 组织结构图 (p39)
            │                      │   │   ├── 📈 IC市场增长图 (p72)
            │                      │   │   ├── 🤖 mimo-v2.5 视觉理解
            │                      │   │   └── 💾 Redis 缓存 (1h)
            │                      │   │
            │                      │   ├─ 👁️ 视觉PDF解析
            │                      │   │   ├── visual_detection 区域检测
            │                      │   │   ├── visual_render 页面渲染
            │                      │   │   ├── visual_extractor 内容提取
            │                      │   │   └── visual_geometry 几何计算
            │                      │   │
            │                      │   ├─ 📦 模块化重构
            │                      │   │   ├── rag_engine/ mixin包 (6模块)
            │                      │   │   ├── retriever/ 包 (6模块)
            │                      │   │   └── utils/llm_engine/ 包 (6模块)
            │                      │   │
            │                      │   ├── 🟢 小米 MiMo LLM 供应商
            │                      │   ├── 🟢 MinerU 强制主后端
            │                      │   └── 🟢 Milvus 文档存储
            │                      │
            │                      ├─ 📁 目录结构
            │                      │   ├── data/ — PDF 原文
            │                      │   ├── 🟢 mineru_output/ — MinerU解析+图片
            │                      │   ├── pdf_parser/ — 含视觉解析模块
            │                      │   ├── 🟢 src/rag_engine/ — mixin包
            │                      │   ├── 🟢 src/retriever/ — 检索器包
            │                      │   ├── 🟢 src/multimodal/ — 多模态模块
            │                      │   ├── 🟢 utils/llm_engine/ — LLM引擎包
            │                      │   └── app.py + main.py — 入口
            │                      │
            │                      ├─ ▶ 运行模式
            │                      │   ├── Web 模式 — streamlit (8502)
            │                      │   ├── CLI 模式 — main.py --mode cli
            │                      │   ├── 工单测试 — test_工单问题.py
            │                      │   └── 🟢 MinerU 独立解析 CLI
            │                      │
            │                      ├─ 🔧 依赖服务
            │                      │   ├── Redis (6379) — 短期记忆+多模态缓存
            │                      │   ├── Milvus (19530) — 向量库+文档存储
            │                      │   ├── 🟢 小米 MiMo API — LLM+多模态
            │                      │   └── MinerU (WSL) — PDF 高精度解析
            │                      │
            │                      ├─ 🚀 部署流程
            │                      │   ├── 1. python -m venv .venv
            │                      │   ├── 2. pip install -r requirements.txt
            │                      │   ├── 3. docker compose up -d
            │                      │   ├── 🟢 4. 配置 .env (小米 MiMo Key)
            │                      │   ├── 5. 放置 PDF 到 data/
            │                      │   ├── 🟢 6. 运行 MinerU 解析
            │                      │   └── 7. python main.py 启动
            │                      │
            │                      └─ ⚙ 配置管理
            │                          ├── 🟢 .env — 小米MiMo Key / 模型选择
            │                          ├── src/config.py — 检索/切块/多模态参数
            │                          └── 🟢 v4 新增配置项
            │                              ├── MULTIMODAL_API_* — 多模态API
            │                              ├── ENABLE_MILVUS_DOCUMENT_STORE
            │                              ├── PDF_PARSE_BACKEND=mineru
            │                              ├── MINERU_FALLBACK_TO_LOCAL
            │                              ├── MULTIMODAL_WORK_ORDER_IMAGES
            │                              └── ENABLE_MULTIMODAL_REDIS_CACHE
            │
            └─ ℹ v3 → v4 升级要点
                ├── 🟢 LLM: SiliconFlow/DeepSeek → 小米 MiMo (mimo-v2.5-pro)
                ├── 🟢 新增多模态图片问答 (mimo-v2.5)
                ├── 🟢 新增视觉PDF解析 (检测/渲染/提取)
                ├── 🟢 代码重构: 单文件 → 包结构 (mixin)
                ├── 🟢 MinerU 成为强制主解析后端
                ├── 🟢 Milvus 支持文档存储
                └── 🟢 Redis 多模态缓存 (1h TTL)
```

### 快速参考：关键路径速查

```
项目结构：
  C:\Users\freedom\Desktop\问答系统4\
  ├── 入口：app.py → Streamlit 前端 (port 8502)
  │        main.py → web/cli 统一入口
  │
  ├── 配置：.env → 小米MiMo Key、模型、路径
  │        src/config.py → 检索、切块、多模态参数
  │
  ├── 数据：data/ → 放入 PDF 文件
  │        mineru_output/ → MinerU 解析结果 + 图片（自动生成）
  │
  ├── 容器：docker compose up -d → Redis + Milvus
  │
  └── 启动：.\.venv\Scripts\python.exe main.py → http://localhost:8502

🚀 v4 新增入口：
  MinerU 解析：python run_mineru_wsl.py data --output-dir mineru_output
  多模态图片：mineru_output/images/ 下的 PNG 文件

端口： 8502(Streamlit) | 6379(Redis) | 19530(Milvus)
```
